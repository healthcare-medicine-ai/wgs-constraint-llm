# Pinned to the versions that produced both the submitted Revision 2 results
# and the corrected results. Python 3.11.4.
#
# These are exact pins rather than minimums on purpose: an external group
# attempting to reproduce this should not have to discover which pandas
# version we ran. Verified on Sherlock, 2026-08-09.

numpy==1.24.3
pandas==1.5.3
scipy==1.11.4
statsmodels==0.14.2
pyBigWig==0.3.24
pyliftover==0.4
matplotlib==3.7.1
seaborn==0.12.2
tqdm==4.65.0
openpyxl==3.1.2

# Optional. config.py falls back to a built-in parser when PyYAML is absent,
# so the pipelines run without it.
PyYAML==6.0.1

# Test suite only.
pytest==7.4.0
